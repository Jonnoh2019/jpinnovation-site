/* JP Innovation final role/menu polish: one role badge system, compact status panel, menu lock and final hover polish. */
(function () {
  const esc = (value) => typeof escapeHtml === "function" ? escapeHtml(value) : String(value ?? "").replace(/[&<>"']/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#039;" })[c]);
  const clean = (value) => typeof cleanEmailValue === "function" ? cleanEmailValue(value || "") : String(value || "").trim().toLowerCase();
  const list = (value) => Array.isArray(value) ? value : [];
  const ROLE_BADGES = {
    admin: { label: "Admin", star: "☆", description: "JP Innovation admin account with moderation, account approval and site-management access." },
    hub: { label: "Hub Member", star: "★", description: "Paid Innovation Hub member with access to member discussions, projects, quotes and directory features." },
    client: { label: "Client Portal", star: "★", description: "Free Client Portal account for quotes, project communication and requests." }
  };
  const REP_BADGES = {
    blue: { label: "Blue Verified", description: "Vetted by JP Innovation." },
    gold: { label: "Gold Trusted", description: "Achieved through approved reviews or contribution points." },
    none: { label: "Reputation pending", description: "Reputation appears after membership and approval checks." }
  };
  const icons = {
    user: '<svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="8" r="4"/><path d="M4 21c.8-5 3.4-7 8-7s7.2 2 8 7"/></svg>',
    mail: '<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 6h16v12H4z"/><path d="m4 7 8 6 8-6"/></svg>'
  };
  function addStyles() {
    if (document.getElementById("jpFinalRoleMenuPolishStyles")) return;
    const style = document.createElement("style");
    style.id = "jpFinalRoleMenuPolishStyles";
    style.textContent = `
      :root{--jp-gold:#f6c945;--jp-blue:#2f8dff;--jp-green:#31d982}
      .member-role-star,.member-status-star,.member-status-star-inline{display:inline-grid!important;place-items:center!important;flex:0 0 auto!important;border-radius:999px!important;font-weight:950!important;line-height:1!important}.member-role-star{width:20px!important;height:20px!important;font-size:12px!important}.member-status-star{width:20px!important;height:20px!important;font-size:12px!important}.member-status-star-inline{width:18px!important;height:18px!important;font-size:11px!important;margin-left:6px!important;position:static!important;transform:none!important}
      .member-role-star.admin,.member-status-star.admin,.member-status-star-inline.admin{color:var(--jp-gold)!important;background:radial-gradient(circle at 50% 50%,rgba(47,141,255,.92) 0 54%,rgba(5,11,18,.96) 55% 100%)!important;border:1.5px solid var(--jp-gold)!important;box-shadow:0 0 0 1px rgba(47,141,255,.25),0 0 14px rgba(246,201,69,.25)!important}.member-role-star.hub,.member-status-star.hub,.member-status-star-inline.hub{color:#201500!important;background:linear-gradient(135deg,#ffe58b,#f5b51d)!important;border:1px solid rgba(255,229,139,.9)!important;box-shadow:0 0 12px rgba(246,201,69,.2)!important}.member-role-star.client,.member-status-star.client,.member-status-star-inline.client{color:#fff!important;background:linear-gradient(135deg,#48a7ff,#0969df)!important;border:1px solid rgba(100,184,255,.9)!important;box-shadow:0 0 12px rgba(47,141,255,.2)!important}
      .member-role-pill{display:inline-flex!important;align-items:center!important;gap:6px!important;min-height:22px!important;padding:5px 9px!important;border-radius:999px!important;font-size:10px!important;font-weight:950!important;letter-spacing:.07em!important;text-transform:uppercase!important;white-space:nowrap!important}.member-role-pill.admin{color:var(--jp-gold)!important;border:1px solid rgba(246,201,69,.5)!important;background:linear-gradient(135deg,rgba(47,141,255,.2),rgba(246,201,69,.09))!important}.member-role-pill.hub{color:#ffe28a!important;border:1px solid rgba(246,201,69,.38)!important;background:rgba(246,201,69,.16)!important}.member-role-pill.client{color:#88c7ff!important;border:1px solid rgba(47,141,255,.42)!important;background:rgba(47,141,255,.11)!important}
      .member-name-line,.compact-member-title-line,.member-name-with-badge{display:flex!important;align-items:center!important;gap:6px!important;min-width:0!important}#memberProfileButton.member-chip{box-sizing:border-box!important;display:grid!important;place-items:center!important;width:42px!important;height:42px!important;min-width:42px!important;max-width:42px!important;min-height:42px!important;max-height:42px!important;aspect-ratio:1/1!important;padding:0!important;border-radius:999px!important;overflow:hidden!important}#memberInitials{display:grid!important;place-items:center!important;width:100%!important;height:100%!important;border-radius:999px!important;line-height:1!important}#memberInitials img{width:100%!important;height:100%!important;object-fit:cover!important;border-radius:999px!important}
      body.jp-menu-hard-lock{position:fixed!important;inset:0!important;width:100%!important;overflow:hidden!important;touch-action:none!important;overscroll-behavior:none!important}body.jp-menu-hard-lock .sidebar,body.jp-menu-hard-lock #memberProfileMenu,body.jp-menu-hard-lock #notificationPopover{touch-action:auto!important;overscroll-behavior:contain!important}.member-profile-menu{overscroll-behavior:contain!important}@media(max-width:760px){#memberProfileMenu.open{position:fixed!important;left:14px!important;right:14px!important;top:var(--hub-mobile-header-offset,176px)!important;bottom:10px!important;max-height:none!important;overflow:auto!important;border-radius:22px!important}.notification-popover.open{position:fixed!important;left:14px!important;right:14px!important;top:var(--hub-mobile-header-offset,176px)!important;width:auto!important;max-width:none!important}}
      .sidebar .nav-link,.profile-menu-link,.directory-account-filter,.member-card,.primary-button,.secondary-button,.dashboard-link{transition:transform .22s ease,border-color .22s ease,background .22s ease,box-shadow .22s ease,color .22s ease}.sidebar .nav-link:hover,.sidebar .nav-link:focus-visible,.profile-menu-link:hover,.profile-menu-link:focus-visible,.directory-account-filter:hover,.directory-account-filter:focus-visible{transform:translateY(-1px);border-color:rgba(47,141,255,.45)!important;background:rgba(47,141,255,.1)!important;outline:none!important}.member-card:hover{transform:translateY(-2px);box-shadow:0 22px 48px rgba(0,0,0,.34),0 0 0 1px rgba(255,255,255,.04) inset}.primary-button:hover,.secondary-button:hover,.dashboard-link:hover{transform:translateY(-1px)}
      .status-guide-card{display:grid;gap:14px}.status-guide-hero{display:grid;gap:8px;padding:14px;border-radius:20px;border:1px solid rgba(47,141,255,.28);background:linear-gradient(145deg,rgba(47,141,255,.13),rgba(255,255,255,.035))}.status-guide-row{display:grid;grid-template-columns:auto 1fr;gap:10px;align-items:start;padding:11px;border-radius:16px;border:1px solid rgba(255,255,255,.08);background:rgba(255,255,255,.035)}.status-guide-row h3{margin:0 0 2px!important;font-size:15px!important}.status-guide-row p{margin:0!important;color:#aeb8c6!important;font-size:12px!important;line-height:1.35!important}.status-guide-section-title{margin:4px 0 -4px!important;color:#7db9ff!important;font-size:11px!important;font-weight:950!important;letter-spacing:.12em!important;text-transform:uppercase!important}@media(prefers-reduced-motion:reduce){*{transition:none!important;animation:none!important;scroll-behavior:auto!important}}
    `;
    document.head.appendChild(style);
  }
  function accountRole(member) { const role = String(member?.role || member?.account_type || "").toLowerCase(); const status = String(member?.membershipStatus || member?.membership_status || "").toLowerCase(); const level = String(member?.level || "").toLowerCase(); if (role === "admin" || level.includes("admin")) return "admin"; if (role === "member" || ["active", "approved", "paid"].includes(status)) return "hub"; return "client"; }
  function reputation(member) { if (typeof memberReputationTier === "function") { const tier = memberReputationTier(member); return tier === "admin" ? "gold" : (["blue", "gold"].includes(tier) ? tier : "none"); } const tier = String(member?.badgeTier || "").toLowerCase(); return ["blue", "gold"].includes(tier) ? tier : "none"; }
  function roleStar(type) { const role = ROLE_BADGES[type] || ROLE_BADGES.client; return `<span class="member-role-star ${esc(type)}" title="${esc(role.label)}" aria-label="${esc(role.label)}">${role.star}</span>`; }
  function rolePill(type) { const role = ROLE_BADGES[type] || ROLE_BADGES.client; return `<span class="member-role-pill ${esc(type)}">${esc(role.label)}</span>`; }
  function normalise(member) { const email = clean(member?.email); return { ...member, id: member?.id || member?.user_id || email || "", user_id: member?.user_id || member?.id || "", email, name: member?.name || member?.full_name || (email ? email.split("@")[0] : "Member"), business: member?.business || member?.company || "", role: member?.role || member?.account_type || "client", membershipStatus: member?.membershipStatus || member?.membership_status || "", skill: member?.skill || member?.skills || member?.preferredWork || "General Engineering", location: member?.location || "Location TBC", bio: member?.bio || member?.about || "Innovation Hub member.", directoryVisible: member?.directoryVisible !== false && member?.directory_visible !== false, status: member?.status || "active" }; }
  function directoryProfiles() { const map = new Map(); const score = (member) => ({ admin: 3, hub: 2, client: 1 })[accountRole(member)] || 0; const add = (raw) => { if (!raw) return; const member = normalise(raw); if (!member.directoryVisible || ["removed", "archived", "suspended"].includes(String(member.status || "").toLowerCase())) return; const key = member.email || member.user_id || clean(member.name + "|" + member.business); if (!key) return; const current = map.get(key); if (!current || score(member) >= score(current)) map.set(key, { ...(current || {}), ...member }); }; if (typeof secureAdminProfiles !== "undefined" && Array.isArray(secureAdminProfiles)) secureAdminProfiles.forEach((profile) => add(typeof secureProfileUser === "function" ? secureProfileUser(profile) : profile)); list(window.state?.users || state?.users).forEach(add); list(window.state?.members || state?.members).forEach(add); return Array.from(map.values()).sort((a, b) => { const order = { admin: 0, hub: 1, client: 2 }; return (order[accountRole(a)] - order[accountRole(b)]) || String(a.name || "").localeCompare(String(b.name || "")); }); }
  function compactMemberCard(member) { const viewer = typeof currentUser === "function" ? currentUser() : null; const type = accountRole(member); const online = typeof isMemberOnline === "function" ? isMemberOnline(member, viewer) : clean(member.email) === clean(viewer?.email); const reviews = typeof approvedReviewsFor === "function" ? approvedReviewsFor(member) : []; const points = Number(member.reputationPoints ?? member.points ?? 0); const avatar = typeof profileAvatarMarkup === "function" ? profileAvatarMarkup(member, "profile-avatar") : `<span class="profile-avatar">${esc(String(member.name || "M").slice(0, 2).toUpperCase())}</span>`; const skills = String(member.skill || "General Engineering").split(",").map((item) => item.trim()).filter(Boolean).slice(0, 3); const main = skills[0] || "General Engineering"; return `<article class="member-card member-compact-card role-${esc(type)}"><div class="compact-member-top"><span class="compact-avatar-wrap ${online ? "is-online" : ""}">${avatar}</span><div class="compact-member-copy"><div class="compact-member-kickers">${rolePill(type)}<span class="compact-status-pill ${online ? "is-online" : ""}">${online ? '<span class="online-dot"></span>Online' : "Offline"}</span></div><div class="compact-member-title-line"><h3 class="compact-member-name">${esc(member.name || "Member")}</h3>${roleStar(type)}</div><p class="compact-member-business">${esc(member.business || "Independent member")}</p></div></div><div class="compact-member-copy"><p class="compact-member-category">${esc(main)}</p><p class="compact-member-location">${esc(member.location || "Location TBC")}</p><p class="compact-member-bio">${esc(member.bio || "Innovation Hub member.")}</p></div><div class="compact-chip-row">${skills.map((skill) => `<span class="pill">${esc(skill)}</span>`).join("")}</div><div class="compact-member-stats"><span>${points} pts</span><span>${reviews.length ? `${reviews.length} review${reviews.length === 1 ? "" : "s"}` : "No reviews yet"}</span></div><div class="compact-member-actions"><button class="secondary-button message-member-button" data-member-email="${esc(member.email || "")}" type="button">${icons.mail} Message</button><button class="primary-button view-profile-button" type="button" data-profile-member-id="${esc(member.id || member.user_id || "")}" data-profile-member-email="${esc(member.email || "")}">${icons.user} View profile</button></div></article>`; }
  function installDirectory() { if (typeof memberCard === "function") memberCard = compactMemberCard; if (typeof bindDirectory !== "function") return; bindDirectory = function bindDirectoryFinal() { addStyles(); const skill = document.querySelector("#skillFilter"), location = document.querySelector("#locationFilter"), verified = document.querySelector("#verifiedFilter"), results = document.querySelector("#directoryResults"), tools = document.querySelector(".directory-tools"); if (!skill || !location || !verified || !results || !tools) return; if (!document.querySelector(".directory-account-filters")) tools.insertAdjacentHTML("afterend", `<div class="directory-account-filters" aria-label="Account filters">${[["all", "All"], ["admin", "Admins"], ["hub", "Hub Members"], ["client", "Client Portal"], ["online", "Online"]].map(([value, label]) => `<button class="directory-account-filter" data-directory-type="${value}" type="button">${label}</button>`).join("")}</div>`); const render = () => { document.querySelectorAll(".directory-account-filter").forEach((button) => button.classList.toggle("active", button.dataset.directoryType === window.jpDirectoryFilter)); const skillTerm = skill.value.trim().toLowerCase(), locationTerm = location.value.trim().toLowerCase(), viewer = typeof currentUser === "function" ? currentUser() : null; const members = directoryProfiles().filter((member) => { const role = accountRole(member); const online = typeof isMemberOnline === "function" ? isMemberOnline(member, viewer) : clean(member.email) === clean(viewer?.email); const typeMatch = !window.jpDirectoryFilter || window.jpDirectoryFilter === "all" || window.jpDirectoryFilter === role || (window.jpDirectoryFilter === "online" && online); const verifiedMatch = !verified.checked || role === "admin" || role === "hub" || member.verified || member.vetted || reputation(member) !== "none"; return typeMatch && verifiedMatch && (!skillTerm || String(member.skill || "").toLowerCase().includes(skillTerm)) && (!locationTerm || String(member.location || "").toLowerCase().includes(locationTerm)); }); const group = (role, title) => { const groupMembers = members.filter((member) => accountRole(member) === role); return groupMembers.length ? `<section class="directory-account-section directory-account-section-${role}"><div class="directory-account-heading"><h3>${title}</h3><span>${groupMembers.length}</span></div><div class="directory-account-cards">${groupMembers.map(compactMemberCard).join("")}</div></section>` : ""; }; results.classList.add("directory-results-grouped"); results.innerHTML = group("admin", "Admin accounts") + group("hub", "Paid Innovation Hub members") + group("client", "Free Client Portal members") || '<p class="muted">No matching members found.</p>'; results.querySelectorAll(".message-member-button").forEach((button) => button.addEventListener("click", () => { if (typeof messageDraftRecipientEmail !== "undefined") messageDraftRecipientEmail = button.dataset.memberEmail || ""; if (typeof activeMessageConversationKey !== "undefined") activeMessageConversationKey = ""; if (typeof renderView === "function") renderView("messages"); })); }; if (!window.jpDirectoryFilter) window.jpDirectoryFilter = "all"; [skill, location].forEach((input) => input.addEventListener("input", render)); verified.addEventListener("change", render); document.querySelectorAll(".directory-account-filter").forEach((button) => button.addEventListener("click", () => { window.jpDirectoryFilter = button.dataset.directoryType || "all"; render(); })); render(); }; }
  function statusPanelHtml(member) { const role = accountRole(member), rep = reputation(member); const roleRows = ["admin", "hub", "client"].map((key) => { const item = ROLE_BADGES[key]; return `<article class="status-guide-row"><span class="member-role-star ${key}">${item.star}</span><div><h3>${esc(item.label)}</h3><p>${esc(item.description)}</p></div></article>`; }).join(""); return `<div class="status-guide-card"><div class="status-guide-hero">${rolePill(role)}<h2 id="reputationStatusTitle">Hub status</h2><p class="muted">Your account role and reputation are shown separately so the Hub is clear for clients, paid members and admins.</p></div><p class="status-guide-section-title">Account roles</p>${roleRows}<p class="status-guide-section-title">Reputation levels</p><article class="status-guide-row"><span class="reputation-badge blue"><span aria-hidden="true">★</span></span><div><h3>Blue Verified</h3><p>Paid Hub member vetted by JP Innovation.</p></div></article><article class="status-guide-row"><span class="reputation-badge gold"><span aria-hidden="true">★</span></span><div><h3>Gold Trusted</h3><p>Reached through approved reviews or site contribution points.</p></div></article><article class="status-guide-row"><span class="member-role-star ${esc(role)}">${ROLE_BADGES[role].star}</span><div><h3>Your current role: ${esc(ROLE_BADGES[role].label)}</h3><p>Your current reputation: ${esc((REP_BADGES[rep] || REP_BADGES.none).label)} — ${esc((REP_BADGES[rep] || REP_BADGES.none).description)}</p></div></article></div>`; }
  function installStatusPanel() { if (typeof openReputationStatusDialog !== "function") return; openReputationStatusDialog = function openRoleStatusDialog() { const user = typeof currentUser === "function" ? currentUser() : null, dialog = document.querySelector("#reputationStatusDialog"), body = document.querySelector("#reputationStatusBody"); if (!user || !dialog || !body) return; addStyles(); body.innerHTML = statusPanelHtml(user); dialog.classList.add("open"); dialog.setAttribute("aria-hidden", "false"); }; }
  function updateHeaderRoleBadge() { const user = typeof currentUser === "function" ? currentUser() : null; if (!user) return; const role = accountRole(user), label = ROLE_BADGES[role].label; [document.querySelector("#memberStatusStarInline"), document.querySelector("#reputationStatusButton")].forEach((badge) => { if (!badge) return; badge.textContent = ROLE_BADGES[role].star; badge.classList.remove("hidden", "blue", "gold", "admin", "hub", "client"); badge.classList.add(role); badge.setAttribute("title", label); badge.setAttribute("aria-label", `Open ${label} status`); }); const roleText = document.querySelector("#memberRole"); if (roleText) roleText.textContent = label; }
  function installSetLoggedInWrapper() { if (typeof setLoggedInView !== "function" || setLoggedInView.jpRoleWrapped) return; const base = setLoggedInView; setLoggedInView = function setLoggedInViewWithRoleBadge() { const result = base.apply(this, arguments); setTimeout(updateHeaderRoleBadge, 0); return result; }; setLoggedInView.jpRoleWrapped = true; }
  let lockY = 0; function applyMenuLock() { const open = document.body.classList.contains("mobile-dashboard-menu-open") || document.body.classList.contains("member-profile-menu-open"); if (open && !document.body.classList.contains("jp-menu-hard-lock")) { lockY = window.scrollY || document.documentElement.scrollTop || 0; document.body.style.top = `-${lockY}px`; document.body.classList.add("jp-menu-hard-lock"); } else if (!open && document.body.classList.contains("jp-menu-hard-lock")) { document.body.classList.remove("jp-menu-hard-lock"); document.body.style.top = ""; window.scrollTo(0, lockY); } }
  function installMenuLock() { if (document.body.dataset.jpFinalMenuLock === "1") return; document.body.dataset.jpFinalMenuLock = "1"; new MutationObserver(applyMenuLock).observe(document.body, { attributes: true, attributeFilter: ["class"] }); window.addEventListener("click", (event) => { if (event.target.closest?.(".sidebar .nav-link, #memberProfileMenu .profile-menu-link")) { setTimeout(() => { if (typeof setMobileDashboardMenuOpen === "function") setMobileDashboardMenuOpen(false); if (typeof setMemberProfileMenuOpen === "function") setMemberProfileMenuOpen(false); applyMenuLock(); }, 0); } }, true); }
  function install() { addStyles(); installDirectory(); installStatusPanel(); installSetLoggedInWrapper(); updateHeaderRoleBadge(); installMenuLock(); }
  window.jpAccountRoles = { ROLE_BADGES, accountRole, roleStar, rolePill, statusPanelHtml };
  install(); window.addEventListener("load", install); window.addEventListener("jp:view-rendered", () => setTimeout(updateHeaderRoleBadge, 0));
})();
